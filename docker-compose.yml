version: "3.9"

services:
  argos:
    build: .
    image: argos:latest
    container_name: argos
    restart: unless-stopped
    ports:
      - "${PORT:-5050}:5050"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - PORT=5050
      - FLASK_DEBUG=0
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://127.0.0.1:5050/api/argos/status"]
      interval: 30s
      timeout: 5s
      start_period: 15s
      retries: 3
    # For China mainland deployment, add dns or network_mode as needed.
